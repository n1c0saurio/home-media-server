#!/bin/sh

# Needs to
# * Check if needed packages (curl, ping?) are installed
# * Install if they're not
# * Detecting automatically which package manager is available (apt-get, apt, dnf, yum or apk)
# * Run the check (received as a parameter? [having a function for each service to check?])
